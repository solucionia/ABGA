fastapi>=0.115,<1
uvicorn[standard]>=0.30,<1
httpx>=0.28,<1
# PostgreSQL (producción: el Postgres de Coolify). Con DATABASE_URL vacío se usa SQLite.
psycopg[binary]>=3.2,<4
# Sólo para pruebas: levanta un PostgreSQL embebido para verificar la capa de datos en local.
# En producción no hace falta (ver backend/scripts/verificar_bd.py).
# pgserver>=0.1
